<h1>Songs</h1>

<% @artists.each do |artist| %>
    <h3><%= link_to artist.name, artist_path(artist) %></h3>
    <% artist.songs.each do |song| %>
        <ul>
          <li><%= link_to "#{artist.name} - #{song.title}", song_path(song) %></li>
        </ul>
    <% end %>
<% end %>